<!-- Edit Modal HTML -->
<div id="addEmployeeModal" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <form action="/configs/meters" method="POST">
                <div class="modal-header">
                    <h4 class="modal-title">Add Rule</h4>
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                </div>
                <div class="modal-body">
                    <label>Meter</label>
                    <select class="form-control" name="meter_id">
                        <option selected>Chọn...</option>
                        <% meters.forEach(meter => { %>
                            <option value="<%= meter.id %>"><%= meter.name %></option>
                        <% }) %>
                    </select>
                    <label>Thông số</label>
                    <select id="inputParameter" class="form-control" name="meter_param_id">
                        <option selected>Chọn...</option>
                        <% parameterMeters.forEach(parameterMeter => { %>
                            <option value="<%= parameterMeter.id %>"><%= parameterMeter.name %></option>
                        <% }) %>
                    </select>
                    <label>Giới hạn định mức</label>
                    <select id="inputOperator" class="form-control" onchange="showDivElement(this)" name="operator">
                        <option selected>Chọn...</option>
                        >
                        <option value="equal">Bằng</option>
                        <option value="greater">Lớn hơn</option>
                        <option value="smaller">Nhỏ hơn</option>
                        <option value="between">Ngưỡng từ</option>
                    </select>
                    <div class="form-group" id="inputNotBetween">
                        <label>Giá trị</label>
                        <input type="number" class="form-control" name="value_single"/>
                    </div>
                    <div class="form-group" id="inputBetween">
                        <label>Giá trị từ</label>
                        <input type="number" class="form-control" name="value_from"/>
                        <label>Giá trị đến</label>
                        <input type="number" class="form-control" name="value_to"/>
                    </div>
                    <div class="form-group">
                        <label>Nội dung</label>
                        <textarea class="form-control" name="message"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <input type="button" class="btn btn-danger" data-dismiss="modal" value="Cancel">
                    <input type="submit" class="btn btn-success" value="Add">
                </div>
            </form>
        </div>
    </div>
</div>